{
  "name": "weedme-slingr-sell-sheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generate OCS sell sheets from Weed Me Slingr purchase orders.",
  "scripts": {
    "dev": "tsx src/server.ts",
    "generate": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
